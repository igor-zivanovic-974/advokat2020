<!-- SIDE-MENU >> SideMenu$: {{hasSideMenu$ | async}} SideMenu: {{hasSideMenu}}
{{((isMobileScreen$ | async) && !(hasSideMenu$ | async) || !(isMobileScreen$ | async))}}
{{((isMobileScreen$ | async) && (hasSideMenu$ | async) || (isMobileScreen$ | async))}} -->
<!-- mobile -->
<div class="side-menu hide side-menu-desktop" [ngClass]="{
    'show side-menu-mobile': ((isMobileScreen$ | async) && !(hasSideMenu$ | async)),
    'show side-menu-desktop': !(isMobileScreen$ | async),
    'hide side-menu-desktop': (isMobileScreen$ | async) && (hasSideMenu$ | async)
  }">
  <img src="assets/images/logo.png" class="ml-4 mt-3 w-75" alt="{{ 'lawyer' | translate }}" />
  <ul class="list-unstyled mt-4">
    <li (contextmenu)="displayContextMenu($event, 'home')">
      <button routerLink="/home" routerLinkActive="active" class="btn w-100 px-2 no-outline text-left" [ngClass]="{
          'btn-outline-link': !(isMobileScreen$ | async),
          'btn-outline-secondary': (isMobileScreen$ | async)
        }" (click)="toggleSideMenu(false)">
        <i class="fa fa-home" routerLinkActive="active2"></i> Home
      </button>
    </li>
    <li (contextmenu)="displayContextMenu($event, 'cases')">
      <button routerLink="/cases" routerLinkActive="active" class="btn w-100 px-2 text-left" [ngClass]="{
          'btn-outline-link': !(isMobileScreen$ | async),
          'btn-outline-secondary': (isMobileScreen$ | async)
        }" (click)="toggleSideMenu(false)">
        <i class="fa fa-folder" routerLinkActive="active2"></i> Cases
      </button>
    </li>
    <li (contextmenu)="displayContextMenu($event, 'clients')">
      <button routerLink="/clients" routerLinkActive="active" class="btn w-100 px-2 text-left" [ngClass]="{
          'btn-outline-link': !(isMobileScreen$ | async),
          'btn-outline-secondary': (isMobileScreen$ | async)
        }" (click)="toggleSideMenu(false)">
        <i class="fa fa-users" routerLinkActive="active2"></i> Clients
      </button>
    </li>
    <li (contextmenu)="displayContextMenu($event, 'employees')">
      <button routerLink="/employees" routerLinkActive="active" class="btn w-100 px-2 text-left" [ngClass]="{
          'btn-outline-link': !(isMobileScreen$ | async),
          'btn-outline-secondary': (isMobileScreen$ | async)
        }" (click)="toggleSideMenu(false)">
        <i class="fa fa-user" routerLinkActive="active2"></i> Employees
      </button>
    </li>
  </ul>
</div>

@import "src/theme/theme-variables";

.side-menu {
  height: 100vh; // 400px;
  //   width: 90%; // 300px;
  color: white;
  // border: solid 2px red;
  z-index: 2;
  // background: $menu-color; // rgba(139, 222, 224, 0.9);
  // border-radius: 4px;
  box-shadow: 0 5px 15px rgba(black, 0.2);
  position: absolute;
  left: 0;
  top: 0;
  //   margin-left: -15px;
  //   margin-right: -15px;
  //   padding-left: 15px;
}

.show {
  //   opacity: 1;
  transition: 1s;
  width: 90%;
  // display: block;
  //   transform: translateY(0);
}

.hide {
  opacity: 0;
  transition: 1s;
  width: 0;
  // left: -100;
  display: none;
  //   transform: translateY(100);
}

.side-menu-mobile {
  background: $menu-color;
}

.side-menu-desktop {
  background: white;
}

// ul li a
.menu-link {
  color: white;
  display: block;
  text-decoration: none;
  font-size: 1.3em;
  font-weight: 400;
  // border: solid 1px yellow;
  margin: 10px 0;
  outline: none;

  &:hover {
    background: #000 !important;
    outline: none;
  }

  &:active {
    background: yellow;
    outline: none;
  }
}

.no-outline {
  outline: none !important;
}

.btn {
  &:focus {
    box-shadow: none;
    // border-bottom: solid 3px olive;
  }

  &:hover {
    background: rgb(196, 170, 26);
  }
}

.active {
  border-bottom: solid 3px olive;
}

.active2 {
  color: olive;
}
